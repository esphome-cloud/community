# /opt/aegis-auto-reply/auto_reply.env — install at 600 root:root.
# Phase 1 Task 1.3 / ADR-009 Path A2 sub-option (a) — auto-reply secrets.
# Rotate quarterly (see CLAUDE.md secret rotation matrix; first rotation 2026-08-10).

IMAP_HOST=imap.163.com
IMAP_USER=ff4415@163.com
IMAP_PASSWORD=<163 client authorization code (16-char from 设置 → POP3/SMTP/IMAP)>
SMTP_HOST=smtp.resend.com
SMTP_USER=resend
SMTP_PASSWORD=<Resend API key (re_...) from resend.com Settings → API Keys>
