# Phase 0 Task 0.4 (rev. Phase 3 ADR-008) — runtime deps for scripts/triage.py.
# Pinned to the latest minor at the time of authoring; bump deliberately.
#
# DeepSeek's API is OpenAI-compatible — use the openai SDK with custom base_url.
# (Anthropic SDK was removed at ADR-008 supersession of ADR-003.)

openai>=1.0,<2.0
PyGithub>=2.5,<3.0

# Indirect runtime — not strictly required for triage.py itself but commonly
# used by GH Actions diagnostics and is safe to install alongside.
typing-extensions>=4.12
